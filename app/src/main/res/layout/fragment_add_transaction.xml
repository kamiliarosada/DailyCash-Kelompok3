<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    tools:context=".ui.transaction.AddTransactionFragment">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingTop="64dp"
        android:paddingBottom="16dp"
        android:paddingHorizontal="16dp"
        app:layout_constraintTop_toTopOf="parent"
        android:background="@drawable/bg_header_gradient">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_revert"
            app:tint="@color/white" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginStart="16dp"
            android:background="@drawable/bg_rounded_pill"
            android:backgroundTint="#33FFFFFF"
            app:tabBackground="@android:color/transparent"
            app:tabIndicator="@drawable/bg_rounded_pill"
            app:tabIndicatorColor="@color/white"
            app:tabIndicatorGravity="stretch"
            app:tabSelectedTextColor="?attr/colorPrimary"
            app:tabTextColor="#CCFFFFFF"
            app:tabRippleColor="@null"
            app:tabTextAppearance="@style/TextAppearance.Material3.LabelLarge">

            <com.google.android.material.tabs.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/tab_expense" />

            <com.google.android.material.tabs.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/tab_income" />
        </com.google.android.material.tabs.TabLayout>
    </LinearLayout>

    <!-- Categories Grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCategories"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="8dp"
        android:clipToPadding="false"
        app:layout_constraintBottom_toTopOf="@+id/inputArea"
        app:layout_constraintTop_toBottomOf="@id/header"
        tools:listitem="@layout/item_category" />

    <!-- Remove customCategoryArea from here -->

    <!-- Input Area -->
    <LinearLayout
        android:id="@+id/inputArea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/cream_surface"
        android:orientation="vertical"
        android:padding="12dp"
        app:layout_constraintBottom_toTopOf="@+id/footerActions">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <EditText
                android:id="@+id/etComment"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@null"
                android:hint="@string/comment_hint"
                android:padding="12dp"
                android:textColor="?attr/colorOnSurface"
                android:textColorHint="?attr/colorOutline"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/btnCurrency"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="IDR ▾"
                android:textColor="@color/primary"
                android:textStyle="bold"
                android:padding="8dp"
                android:background="@drawable/bg_rounded_pill"
                android:backgroundTint="#1A000000" />

            <EditText
                android:id="@+id/etAmount"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1.2"
                android:background="@null"
                android:hint="0"
                android:inputType="none"
                android:padding="12dp"
                android:textAlignment="viewEnd"
                android:textColor="?attr/colorPrimary"
                android:textSize="24sp"
                android:textStyle="bold" />
        </LinearLayout>
    </LinearLayout>

    <!-- Footer Actions (Date) -->
    <LinearLayout
        android:id="@+id/footerActions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingVertical="4dp"
        android:background="@color/cream_surface"
        app:layout_constraintBottom_toTopOf="@+id/keypad">

        <TextView
            android:id="@+id/btnDate"
            style="@style/SideButton"
            android:layout_marginTop="0dp"
            android:backgroundTint="@color/vanilla_bg"
            android:drawableStart="@android:drawable/ic_menu_my_calendar"
            android:text="@string/today" />
    </LinearLayout>

    <!-- Keypad (Smaller) -->
    <GridLayout
        android:id="@+id/keypad"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/vanilla_bg"
        android:columnCount="4"
        android:padding="2dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button android:id="@+id/btn1" style="@style/KeypadButtonSmall" android:text="1" />
        <Button android:id="@+id/btn2" style="@style/KeypadButtonSmall" android:text="2" />
        <Button android:id="@+id/btn3" style="@style/KeypadButtonSmall" android:text="3" />
        <View android:layout_width="0dp" android:layout_height="0dp" android:layout_columnWeight="1" />

        <Button android:id="@+id/btn4" style="@style/KeypadButtonSmall" android:text="4" />
        <Button android:id="@+id/btn5" style="@style/KeypadButtonSmall" android:text="5" />
        <Button android:id="@+id/btn6" style="@style/KeypadButtonSmall" android:text="6" />
        <ImageButton android:id="@+id/btnMinus" style="@style/KeypadImageButtonSmall" android:src="@android:drawable/ic_input_delete" />

        <Button android:id="@+id/btn7" style="@style/KeypadButtonSmall" android:text="7" />
        <Button android:id="@+id/btn8" style="@style/KeypadButtonSmall" android:text="8" />
        <Button android:id="@+id/btn9" style="@style/KeypadButtonSmall" android:text="9" />
        <ImageButton android:id="@+id/btnDelete" style="@style/KeypadImageButtonSmall" android:src="@android:drawable/ic_delete" />

        <Button android:id="@+id/btn0" style="@style/KeypadButtonSmall" android:text="0" />
        <Button android:id="@+id/btnDot" style="@style/KeypadButtonSmall" android:text="." />
        <View android:layout_width="0dp" android:layout_height="0dp" android:layout_columnWeight="1" />
        <Button
            android:id="@+id/btnSave"
            style="@style/KeypadButtonSmall"
            android:text="TAMBAH"
            android:backgroundTint="@color/primary"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold" />

    </GridLayout>

    <!-- Remove Floating Actions Container -->


</androidx.constraintlayout.widget.ConstraintLayout>
