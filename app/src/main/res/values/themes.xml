<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyCash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_brown</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/primary_light_brown</item>
        <item name="android:windowBackground">@color/cream_bg</item>
        <item name="colorSurface">@color/cream_surface</item>
        <item name="colorOnSurface">@color/text_main</item>
        <item name="colorOutlineVariant">@color/card_border</item>
        <item name="android:statusBarColor">@color/primary_brown</item>

        <!-- Bottom Nav Colors -->
        <item name="colorSurfaceVariant">@color/cream_surface</item>
    </style>

    <style name="Theme.DailyCash" parent="Base.Theme.DailyCash" />

    <!-- Extra aesthetic styles -->
    <style name="ExtraRoundedCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">28dp</item>
        <item name="cardElevation">0dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/card_border</item>
    </style>
</resources>